name: sp_dart
description: "A new Dart FFI package project."
version: 0.0.1

environment:
  sdk: ^3.12.1

dependencies:
  hooks: ^1.0.0
  ffi: ^2.1.3
  native_toolchain_rust: ^1.0.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
